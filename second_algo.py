# define T is totaled time 
T = []
T.append(swim)
T.append(bike)
T.append(run)
I = []

while not T: 
    if not T:
        break
    else :
        next = max(T)
        sorted(next)
        I.append(next)
        T.remove(next)
    